
#어노테이션
어노테이션이란 Java Enterprise Edition 5부터 추가된 새로운 문법이다.
'주석'이라는 의미를 가지고 있으면 코드에 주석처럼 달아서 의미를 부여한다.
어노테이션은 컴파일/런타임시에 읽힌다. 

아래는 자주 사용하는 어노테이션에 대한 정의이다.

@Autowired:
자동으로 해당 타입의 Bean(ApplicationContext가 관리하는 Bean)을 DI(의존성 주입)한다.

@Qualifier:
@Autowired와 함께 사용되면 지정해준 동일 타입의 Bean객체가 존재할 떼 해당하는 그 특정빈을 삽입할 수 있게 해준다.

예) 
@Qualifier("bean_id")

