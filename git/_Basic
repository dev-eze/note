워킹 트리= 디렉토 = 작업 디렉토리
.git이 있는 디렉토리를 워킹 트리라 한다.


명령어
clone: 프로젝트를 원격 저장소 -> 로컬 저장소로 복제한다.
init
add: 변경한 file을 Stage에 올린다.
commit: Stage에 있는 파일들을 로컬 저장소(.git)에 저장 한다. 내 워킹트리에 SnapShot이라고 한다.
reset: 이전 커밋으로 되돌릴 때 사용된다.  
  --soft
  --mix
  --hard
revert: 
checkout: (detached HEAD)를 가져와서 브랜치를 변경한다.
branch는 commit의 참조변수이다. 
rebase: 현재 브랜치에서 대상 브랜치와 차이가 나는 커밋을 가져온다.
push: 로컬 저장소를 원격저장소에 올려 반영한다. 
pull: 원격저장소를 로컬 저장소애 복사한다. (fetch + merge)
현재 작업 디렉토리에 마지막 커밋 내용을 반영한다.
fetch: 저장소에 없는 커밋들을 온다.
